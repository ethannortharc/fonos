## Summary

<!-- What does this change do, and why? -->

## Linked issue

<!-- Closes #... , or "N/A" -->

## How verified

<!-- Commands you ran, e.g.:
cd fonos-desktop && npm test
cargo test --workspace --features ci
cd fonos-desktop && npm run build
-->

## Checklist

- [ ] Any new user-facing string was added to **both** `en` and `zh` in
      `fonos-desktop/src/lib/i18n.tsx`
- [ ] Screenshots attached, if this changes UI
