import Testing
@testable import FonosApp

/// STT provider tests — implementations go in wp-executor.
struct STTServiceTests {
    @Test func testAppleSTT() {
        // Scaffold placeholder
        #expect(Bool(true))
    }

    @Test func testWhisperSTT() {
        // Scaffold placeholder
        #expect(Bool(true))
    }
}
